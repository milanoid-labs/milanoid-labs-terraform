# Copy to secrets.auto.tfvars (gitignored) and fill in real values.
# OpenTofu loads *.auto.tfvars automatically on every plan/apply.

devops_study_app_pat = "<PAT with repo and workflow scopes>"
home_dashboard_pat   = "<PAT with repo and workflow scopes>"
nexus_username       = "<nexus username>"
nexus_password       = "<nexus password>"
